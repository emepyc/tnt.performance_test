var url = "mongodb://localhost:27017/mytestdb";
var mongo = require('mongodb').MongoClient;

var get_blocks = function (name, from, to) {

    var blocks;

    var collection = db.collection("testData");


    return blocks;
};

if (module && module.exports) {
    module.exports = get_blocks;
}
